---
apiVersion: v1
kind: Endpoints
metadata:
  name: ext-tailscale-router
  labels:
    app: tailscale-router
subsets:
  - addresses:
      - ip: 10.0.101.10
      - ip: 10.0.101.11
      - ip: 10.0.101.12
    ports:
      - name: metrics
        port: 9100
---
apiVersion: v1
kind: Service
metadata:
  name: ext-tailscale-router
  labels:
    app: tailscale-router
spec:
  ports:
    - name: metrics
      port: 9100
      targetPort: metrics
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ext-tailscale-router
  labels:
    prometheus.io/monitor: "true"
spec:
  endpoints:
    - port: metrics
  namespaceSelector:
    matchNames:
      - prometheus-exporter
  selector:
    matchLabels:
      app: tailscale-router

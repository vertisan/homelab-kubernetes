---
apiVersion: v1
kind: Endpoints
metadata:
  name: ext-postgresql
  labels:
    app: postgresql
subsets:
  - addresses:
      - ip: 10.0.107.10
      - ip: 10.0.107.11
      - ip: 10.0.107.12
    ports:
      - name: metrics
        port: 9100
---
apiVersion: v1
kind: Service
metadata:
  name: ext-postgresql
  labels:
    app: postgresql
spec:
  ports:
    - name: metrics
      port: 9100
      targetPort: metrics
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ext-postgresql
  labels:
    prometheus.io/monitor: "true"
spec:
  endpoints:
    - port: metrics
  namespaceSelector:
    matchNames:
      - prometheus-exporter
  selector:
    matchLabels:
      app: postgresql

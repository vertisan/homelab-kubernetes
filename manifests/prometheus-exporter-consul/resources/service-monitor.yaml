apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: prometheus-exporter-consul
  labels:
    prometheus.io/monitor: "true"
spec:
  selector:
    matchLabels:
      app: prometheus-exporter-consul
  jobLabel: prometheus-exporter-consul
  endpoints:
    - port: http
      path: /metrics
      interval: 60s
      scrapeTimeout: 40s
